  <div class="container my-5">
    <div class="row">
      <table class="col-md-3 table table-bordered">
        <thead>
          <tr>
            <th>ジャンル検索</th>
          </tr>
        </thead>
        <tbody>
          <% @genres.each do |genre| %>
          <tr>
            <td><%= genre.name %></td>
          </tr>
          <% end %>
        </tbody>
    </table>
      <div class="offset-md-1 col-md-6 border border-dark py-3">
        <p>ようこそ、ながのCAKEへ！</br>
        このサイトはケーキ販売のECサイトになります。</br>
        会員でない方も商品の閲覧はできますが、</br>
        購入には会員登録が必要です。
        </p>
      </div>
    </div>
    <div class="row">
      <h2 class="offset-md-4 col-md-3">新着商品</h2>
      <div class="offset-md-4 col-md-8">
        <% @items. first(4).each do |item| %>
          <div class="item mr-5">
            <%= image_tag attachment_url(item, :image, :fill, 100, 100) %>
            <p><%= item.name %></p>
            <p><%= item.price %></p>
          </div>
        <% end %>
      </div>
    </div>
    <%= link_to "全ての商品を表示", items_path%>
  </div>


